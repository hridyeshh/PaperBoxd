"use client";

// Sidebar removed — navigation is now in the top bar (MinimalDesktopHeader).
// This component is kept as a no-op so existing page JSX doesn't need changes.
export function DesktopSidebar() {
  return null;
}
